# Dragon Forest - Unit Testing
#### Python 3.7+ and PyTest (latest)

Tests are in /tests

pytest test_<name>.py  -- runs single file of tests
pytest                 -- runs all tests with test_* prefixed files and functions

